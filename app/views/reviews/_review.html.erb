<div id="<%=r.id%>">
    <%= r.body %>
    <td><%= link_to 'Edit', edit_movie_review_path(@movie,r),remote: true, :class => 'btn btn-primary review-edit', data: { bs_toggle: "modal", bs_target: "#exampleModal"} if can? :edit ,r %></td>
    <td><%= link_to 'Destroy', movie_review_path(@movie,r), method: :delete, data: { confirm: 'Are you sure?' }, remote: true if can? :destroy,r%></td>
</div>